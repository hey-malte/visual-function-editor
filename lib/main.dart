import 'package:flutter/material.dart';

void main() {
  runApp(const VisualFunctionEditorApp());
}

class VisualFunctionEditorApp extends StatelessWidget {
  const VisualFunctionEditorApp({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}